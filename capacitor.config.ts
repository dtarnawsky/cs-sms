import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.sms',
  appName: 'cs-sms',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
